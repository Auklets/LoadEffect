import React, { PropTypes } from 'react';

const LiveResults = (props) => {

  return (
    <div>
      {'Hi this is the live results page'}
      <div id="testChart">
      </div>
    </div>
  );
};

LiveResults.propTypes = {
};

export default LiveResults;
